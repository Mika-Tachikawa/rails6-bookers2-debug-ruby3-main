<table class='table'>
  <tr><%= image_tag user.get_profile_image, size:'100x100' %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
    <tr>
        <th><p>follows</p></th>
    </tr>
    <tr>
        <th><P>followers</P></th>
    </tr>
</table>

      <% if current_user.following?(user) %>
          <%= link_to "フォロー外す", user_path(user), method: :delete %>
      <% else %>
          <%= link_to "フォローする", user_path(user), method: :post %>
      <% end %>
　
<div class='row'>
  <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block" do %>
    <i class="fas fa-user-cog"></i>
  <% end %>
</div>